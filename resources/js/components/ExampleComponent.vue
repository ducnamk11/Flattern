<template>

        <h1>const router = new VueRouter({ routes // short for `routes: routes` // 4. Create and mount the rnst router =
            new VueRouter({ routes // short for `routes: routes` // 4. Create and mount the rnst router = new
            VueRouter({ routes // short for `routes: routes` // 4. Create and mount the rnst router = new VueRouter({
            routes // short for `routes: routes` // 4. Create and mount the rnst router = new VueRouter({ routes //
            short for `routes: routes` // 4. Create and mount the rnst router = new VueRouter({ routes // short for
            `routes: routes` // 4. Create and mount the root instance.
        </h1>

</template>

